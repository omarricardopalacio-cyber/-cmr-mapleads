import type { MemoryResult } from "./types"

export async function loadKnowledgeMemory(): Promise<MemoryResult> {
  return {
    success: true,
    message: "V2 knowledge memory placeholder — no-op",
  }
}
